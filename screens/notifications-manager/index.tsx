import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

export const NotificationsManager = (props: Props) => {
  return (
    <View>
      <Text>NotificationsManager</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
