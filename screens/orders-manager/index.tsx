import { View, Text } from "react-native";
import React from "react";

type Props = {};

export const OrdersManager = (props: Props) => {
  return (
    <View>
      <Text>OrderManager</Text>
    </View>
  );
};
